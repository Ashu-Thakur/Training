# !thread =>A thread is a separate flow of execution. This means that your program will have two things happening at once. But for most Python 3 implementations the different threads do not actually execute at the same time: they merely appear to

# !Concurrency is the ability of a system to handle multiple tasks at the same time by interleaving execution. Tasks progress in overlapping time periods, not necessarily simultaneously.like Threading

# ! Parallelism is the ability to perform multiple tasks simultaneously, which requires multiple CPU cores or processors.
